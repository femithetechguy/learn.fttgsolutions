# Copy this file to .env.local and fill in your values
# .env.local is gitignored — never commit real secrets

NEXT_PUBLIC_SITE_URL=https://learn.fttgsolutions.com

# YouTube Data API v3 key — used only by scripts/scaffold-course.ts (never at runtime)
# Enable at: https://console.cloud.google.com → APIs & Services → YouTube Data API v3
YOUTUBE_API_KEY=
